
###############################################################################
# WARNING                                                                     #
# This file is autogenerated. Do NOT edit this file. Run =./build.py= instead #
###############################################################################

trigger:
  - master
pr:
  autoCancel: true
  branches:
    include:
      - master
pool:
  vmImage: ubuntu-latest
jobs:
  - job:
    displayName: Building the image
    pool:
      vmImage: ubuntu-latest
    steps:
      - script: |
          set -e
          docker build -f Dockerfile -t mercur3/fullstack-app:latest .
          echo "------------------------------------------------------\n"
        displayName: docker build -f Dockerfile -t mercur3/fullstack-app:latest .

      - script: |
          echo "------------------------------------------------------\n"
          echo "$DOCKER_PASSWORD" | docker login -u mercur3 --password-stdin
          docker push mercur3/fullstack-app:latest
        displayName: docker push mercur3/fullstack-app:latest
        condition: not(eq(variables['Build.Reason'], 'PullRequest'))
        env:
          DOCKER_PASSWORD: $(DOCKER_PASSWORD)


